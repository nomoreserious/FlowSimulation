<UserControl x:Class="FlowSimulation.ViewPort.SpectralDensity.SpectralDensityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ex="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="600">
    <ex:ZoomControl Mode="Fill">
        <Grid Width="{Binding Path=Width,Mode=OneWay}" Height="{Binding Path=Height,Mode=OneWay}">
            <Border Opacity="0.4" Background="{Binding Path=Background}"/>
            <Border Background="{Binding Path=SpectralBrush}"/>
        </Grid>
    </ex:ZoomControl>
</UserControl>
